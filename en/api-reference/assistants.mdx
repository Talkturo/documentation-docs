---
title: Assistants
description: Create, update, clone, delete, and manage AI voice assistants through the Talkturo REST API — configure prompts, voices, models, and telephony settings.
---

## Manage assistants

Create assistants, update their configuration, clone existing setups, delete assistants you no longer need, and retrieve usage data for reporting or operational checks.

<Callout kind="info">

All assistant endpoints support dual authentication through `withApiAuth`. Authenticate with either a Talkturo API key or an authenticated session cookie.

</Callout>

### Base path

All endpoints on this page are rooted at ` /api/assistants `.

### Authentication

Send your API key in the `Authorization` header as a bearer token.

<Request tabs="cURL" default-tab="1">

```bash
curl -X POST "https://api.talkturo.com/api/assistants" \
  -H "Authorization: Bearer tk_live_example_9xmk7q2r4f8n6p1s3d5h7j9k2m4q6w8" \
  -H "Content-Type: application/json" \
  -d '{
    "accountSlug": "acme-sales",
    "name": "Inbound Sales Assistant"
  }'
```

</Request>

<Response tabs="200" default-tab="1">

```json
{
  "success": true,
  "assistant": {
    "id": "7f5a4d6e-1d1b-4f76-9b03-2e9f48d09f6a",
    "name": "Inbound Sales Assistant"
  }
}
```

</Response>

If you use session-based auth from the Talkturo app, the browser sends the session cookie automatically. External integrations usually use API keys.

## Create an assistant

Create a new assistant under an account by sending the account slug and assistant name. If you provide a template ID, Talkturo sets the assistant type from that template.

<Request tabs="cURL,JavaScript,Python" default-tab="1">

```bash
curl -X POST "https://api.talkturo.com/api/assistants" \
  -H "Authorization: Bearer tk_live_example_9xmk7q2r4f8n6p1s3d5h7j9k2m4q6w8" \
  -H "Content-Type: application/json" \
  -d '{
    "accountSlug": "acme-sales",
    "name": "Inbound Sales Assistant",
    "templateId": "tmpl_sales_inbound_v1"
  }'
```

```javascript
const response = await fetch("https://api.talkturo.com/api/assistants", {
  method: "POST",
  headers: {
    "Authorization": "Bearer tk_live_example_9xmk7q2r4f8n6p1s3d5h7j9k2m4q6w8",
    "Content-Type": "application/json"
  },
  body: JSON.stringify({
    accountSlug: "acme-sales",
    name: "Inbound Sales Assistant",
    templateId: "tmpl_sales_inbound_v1"
  })
});

const data = await response.json();
console.log(data);
```

```python
import requests

response = requests.post(
    "https://api.talkturo.com/api/assistants",
    headers={
        "Authorization": "Bearer tk_live_example_9xmk7q2r4f8n6p1s3d5h7j9k2m4q6w8",
        "Content-Type": "application/json"
    },
    json={
        "accountSlug": "acme-sales",
        "name": "Inbound Sales Assistant",
        "templateId": "tmpl_sales_inbound_v1"
    }
)

print(response.json())
```

</Request>

<Response tabs="200" default-tab="1">

```json
{
  "success": true,
  "assistant": {
    "id": "7f5a4d6e-1d1b-4f76-9b03-2e9f48d09f6a",
    "account_slug": "acme-sales",
    "name": "Inbound Sales Assistant",
    "type": "sales",
    "created_at": "2025-02-18T14:22:31.114Z",
    "updated_at": "2025-02-18T14:22:31.114Z"
  }
}
```

</Response>

### Endpoint

`POST /api/assistants`

### Body parameters

<ParamField body="accountSlug" param-type="string" required="true">
The account slug that owns the assistant.
</ParamField>

<ParamField body="name" param-type="string" required="true">
The assistant name.
</ParamField>

<ParamField body="templateId" param-type="string" required="false">
An optional template identifier. When present, Talkturo sets the assistant type from the template.
</ParamField>

### Response fields

<ResponseField name="success" field-type="boolean" required="true">
Returns `true` when the assistant is created successfully.
</ResponseField>

<ResponseField name="assistant" field-type="object" required="true">
The newly created assistant record.
</ResponseField>

<ResponseField name="assistant.id" field-type="string" required="true">
The assistant UUID.
</ResponseField>

<ResponseField name="assistant.name" field-type="string" required="true">
The assistant name stored by Talkturo.
</ResponseField>

<ResponseField name="assistant.type" field-type="string" required="false">
The assistant type derived from the template when `templateId` is provided.
</ResponseField>

Talkturo creates the assistant with default or null values for fields you do not provide at creation time. Update the assistant afterward to configure prompts, voices, models, or telephony settings.

## Update an assistant

Update an existing assistant by assistant ID. The update endpoint accepts `accountSlug` and any assistant fields supported by the backend.

<Request tabs="cURL,JavaScript" default-tab="1">

```bash
curl -X PATCH "https://api.talkturo.com/api/assistants/7f5a4d6e-1d1b-4f76-9b03-2e9f48d09f6a" \
  -H "Authorization: Bearer tk_live_example_9xmk7q2r4f8n6p1s3d5h7j9k2m4q6w8" \
  -H "Content-Type: application/json" \
  -d '{
    "accountSlug": "acme-sales",
    "name": "Inbound Sales Assistant v2",
    "voice": "alloy",
    "model": "gpt-4o-realtime"
  }'
```

```javascript
const response = await fetch(
  "https://api.talkturo.com/api/assistants/7f5a4d6e-1d1b-4f76-9b03-2e9f48d09f6a",
  {
    method: "PATCH",
    headers: {
      "Authorization": "Bearer tk_live_example_9xmk7q2r4f8n6p1s3d5h7j9k2m4q6w8",
      "Content-Type": "application/json"
    },
    body: JSON.stringify({
      accountSlug: "acme-sales",
      name: "Inbound Sales Assistant v2",
      voice: "alloy",
      model: "gpt-4o-realtime"
    })
  }
);

const data = await response.json();
console.log(data);
```

</Request>

<Response tabs="200" default-tab="1">

```json
{
  "success": true,
  "assistant": {
    "id": "7f5a4d6e-1d1b-4f76-9b03-2e9f48d09f6a",
    "name": "Inbound Sales Assistant v2",
    "voice": "alloy",
    "model": "gpt-4o-realtime",
    "updated_at": "2025-02-18T14:31:09.027Z"
  }
}
```

</Response>

### Endpoint

`PATCH /api/assistants/{assistantId}`

### Path parameters

<ParamField path="assistantId" param-type="string" required="true">
The assistant UUID.
</ParamField>

### Body parameters

<ParamField body="accountSlug" param-type="string" required="false">
The owning account slug. Include it when your integration scopes updates to a specific account context.
</ParamField>

<ParamField body="assistant fields" param-type="object" required="false">
Any assistant fields accepted by the backend. Use this endpoint to update configuration such as prompts, voices, models, and telephony-related settings.
</ParamField>

### Response fields

<ResponseField name="success" field-type="boolean" required="true">
Returns `true` when the assistant is updated successfully.
</ResponseField>

<ResponseField name="assistant" field-type="object" required="true">
The updated assistant record.
</ResponseField>

<Callout kind="info">

If you send `transcriptor_options`, Talkturo validates those options against the selected provider and model before saving the update.

</Callout>

When the update succeeds, the response includes the saved assistant object. Use that object as the source of truth for the assistant's latest configuration.

## Delete an assistant

Delete an assistant permanently by ID. Talkturo checks ownership before removing the record.

<Request tabs="cURL" default-tab="1">

```bash
curl -X DELETE "https://api.talkturo.com/api/assistants/7f5a4d6e-1d1b-4f76-9b03-2e9f48d09f6a" \
  -H "Authorization: Bearer tk_live_example_9xmk7q2r4f8n6p1s3d5h7j9k2m4q6w8"
```

</Request>

<Response tabs="200" default-tab="1">

```json
{
  "success": true,
  "message": "Assistant deleted successfully"
}
```

</Response>

### Endpoint

`DELETE /api/assistants/{assistantId}`

### Path parameters

<ParamField path="assistantId" param-type="string" required="true">
The assistant UUID to delete.
</ParamField>

### Response fields

<ResponseField name="success" field-type="boolean" required="true">
Returns `true` when the assistant is deleted successfully.
</ResponseField>

<ResponseField name="message" field-type="string" required="true">
A confirmation message. The value is `Assistant deleted successfully`.
</ResponseField>

<Callout kind="alert">

Deleting an assistant is irreversible. Remove it only after you confirm that no active workflows, phone numbers, or operational processes still depend on it.

</Callout>

## Clone an assistant

Clone an existing assistant to duplicate its configuration into a new assistant record. Use cloning when you want a starting point for a variant instead of rebuilding settings manually.

<Request tabs="cURL,JavaScript" default-tab="1">

```bash
curl -X POST "https://api.talkturo.com/api/assistants/7f5a4d6e-1d1b-4f76-9b03-2e9f48d09f6a/clone" \
  -H "Authorization: Bearer tk_live_example_9xmk7q2r4f8n6p1s3d5h7j9k2m4q6w8"
```

```javascript
const response = await fetch(
  "https://api.talkturo.com/api/assistants/7f5a4d6e-1d1b-4f76-9b03-2e9f48d09f6a/clone",
  {
    method: "POST",
    headers: {
      "Authorization": "Bearer tk_live_example_9xmk7q2r4f8n6p1s3d5h7j9k2m4q6w8"
    }
  }
);

const data = await response.json();
console.log(data);
```

</Request>

<Response tabs="200" default-tab="1">

```json
{
  "success": true,
  "assistant": {
    "id": "0f3f8bc6-5e95-4344-88d2-8e5660ec9bf1",
    "name": "Inbound Sales Assistant (Copy)",
    "created_at": "2025-02-18T14:39:12.442Z",
    "updated_at": "2025-02-18T14:39:12.442Z"
  }
}
```

</Response>

### Endpoint

`POST /api/assistants/{assistantId}/clone`

### Path parameters

<ParamField path="assistantId" param-type="string" required="true">
The assistant UUID to clone.
</ParamField>

### Body parameters

<ParamField body="accountSlug" param-type="string" required="false">
The account slug for the workspace where the clone should be created. If omitted, the API key's associated account is used.
</ParamField>

### Response fields

<ResponseField name="success" field-type="boolean" required="true">
Returns `true` when the assistant is cloned successfully.
</ResponseField>

<ResponseField name="assistant" field-type="object" required="true">
The cloned assistant record.
</ResponseField>

<ResponseField name="assistant.id" field-type="string" required="true">
The UUID assigned to the cloned assistant.
</ResponseField>

After cloning, update the new assistant if you need to change the name, prompts, or telephony configuration before using it in production.

## Retrieve usage statistics

Fetch usage data for a single assistant. Use this endpoint for monitoring, dashboards, or account-level reporting workflows.

<Request tabs="cURL,JavaScript" default-tab="1">

```bash
curl -X GET "https://api.talkturo.com/api/assistants/7f5a4d6e-1d1b-4f76-9b03-2e9f48d09f6a/usage-stats?accountId=9a6e7d1c-3fd1-4af6-a3ab-b0e9d3ac4a71" \
  -H "Authorization: Bearer tk_live_example_9xmk7q2r4f8n6p1s3d5h7j9k2m4q6w8"
```

```javascript
const response = await fetch(
  "https://api.talkturo.com/api/assistants/7f5a4d6e-1d1b-4f76-9b03-2e9f48d09f6a/usage-stats?accountId=9a6e7d1c-3fd1-4af6-a3ab-b0e9d3ac4a71",
  {
    method: "GET",
    headers: {
      "Authorization": "Bearer tk_live_example_9xmk7q2r4f8n6p1s3d5h7j9k2m4q6w8"
    }
  }
);

const data = await response.json();
console.log(data);
```

</Request>

<Response tabs="200" default-tab="1">

```json
{
  "totalCalls": 148,
  "totalMinutes": 912.0,
  "avgDurationMinutes": 6.2,
  "completedCallsWithDuration": 145,
  "successRatePercent": 87.5,
  "successEvaluatedCount": 130,
  "timeline": {
    "today": 5,
    "last7Days": 45,
    "last30Days": 148,
    "last365Days": 148
  },
  "dailySeries": [
    { "date": "2025-02-18", "count": 6 },
    { "date": "2025-02-17", "count": 4 }
  ]
}
```

</Response>

### Endpoint

`GET /api/assistants/{assistantId}/usage-stats`

### Path parameters

<ParamField path="assistantId" param-type="string" required="true">
The assistant UUID whose usage statistics you want to retrieve.
</ParamField>

### Query parameters

<ParamField query="accountId" param-type="string" required="true">
The account UUID that owns the assistant. Returns `400` if omitted.
</ParamField>

### Response fields

<ResponseField name="totalCalls" field-type="integer" required="true">
Total number of calls handled by the assistant.
</ResponseField>

<ResponseField name="totalMinutes" field-type="number" required="true">
Total call duration in minutes, rounded to one decimal place.
</ResponseField>

<ResponseField name="avgDurationMinutes" field-type="number" required="false">
Average call duration in minutes. Returns `null` when no calls have both a start and end time recorded.
</ResponseField>

<ResponseField name="completedCallsWithDuration" field-type="integer" required="true">
Number of calls that have both `started_at` and `ended_at` timestamps, used to compute the average.
</ResponseField>

<ResponseField name="successRatePercent" field-type="number" required="false">
Percentage of calls that were successful, rounded to one decimal. Returns `null` when no calls could be scored.
</ResponseField>

<ResponseField name="successEvaluatedCount" field-type="integer" required="true">
Number of calls that could be evaluated for success scoring.
</ResponseField>

<ResponseField name="timeline" field-type="object" required="true">
Call counts grouped by time range: `today`, `last7Days`, `last30Days`, and `last365Days`.
</ResponseField>

<ResponseField name="dailySeries" field-type="array" required="true">
Array of daily call counts for the trailing 30 days. Each entry contains a date in `YYYY-MM-DD` UTC format and a count.
</ResponseField>

## Tool configurations

Tool configs let you attach third-party tools such as Cal.com scheduling, Zapier, or custom webhooks to an assistant.

<Callout kind="info">

All tool configuration endpoints require session authentication.

</Callout>

| Method | Path | Purpose |
| --- | --- | --- |
| `GET` | `/api/assistants/{assistantId}/tool-configs` | List all enabled tool configs |
| `GET` | `/api/assistants/{assistantId}/tool-configs/{toolType}` | Get configs for a specific tool type |
| `POST` | `/api/assistants/{assistantId}/tool-configs/{toolType}` | Create or update a tool config |
| `DELETE` | `/api/assistants/{assistantId}/tool-configs/{toolType}` | Delete tool config(s) |

### Get tool configs by type

Use this endpoint to retrieve one config, or all configs, for a specific tool type.

### Endpoint

`GET /api/assistants/{assistantId}/tool-configs/{toolType}`

### Path parameters

<ParamField path="assistantId" param-type="string" required="true">
The assistant UUID.
</ParamField>

<ParamField path="toolType" param-type="string" required="true" enum="cal_com,zapier,cal_com_schedule,cal_com_get_bookings,cal_com_cancel">
The tool type to retrieve.
</ParamField>

### Query parameters

<ParamField query="configId" param-type="string" required="false">
Optional config UUID to retrieve a specific configuration.
</ParamField>

<ParamField query="all" param-type="boolean" required="false">
Set to `true` to return all configs of this type.
</ParamField>

### Create or update a tool config

Use this endpoint to create a new tool config or update an existing one for the selected tool type.

### Endpoint

`POST /api/assistants/{assistantId}/tool-configs/{toolType}`

### Path parameters

<ParamField path="assistantId" param-type="string" required="true">
The assistant UUID.
</ParamField>

<ParamField path="toolType" param-type="string" required="true" enum="cal_com,zapier,cal_com_schedule,cal_com_get_bookings,cal_com_cancel">
The tool type to create or update.
</ParamField>

### Query parameters

<ParamField query="configId" param-type="string" required="false">
Optional config UUID. When provided, the request updates that existing configuration.
</ParamField>

### Body parameters

<ParamField body="config" param-type="object" required="true">
Free-form configuration for the tool.
</ParamField>

<ParamField body="is_enabled" param-type="boolean" required="false">
Whether the tool config is enabled. Defaults to `true`.
</ParamField>

### Delete tool configs

Delete one config by ID, or remove all configs for a tool type.

### Endpoint

`DELETE /api/assistants/{assistantId}/tool-configs/{toolType}`

### Path parameters

<ParamField path="assistantId" param-type="string" required="true">
The assistant UUID.
</ParamField>

<ParamField path="toolType" param-type="string" required="true" enum="cal_com,zapier,cal_com_schedule,cal_com_get_bookings,cal_com_cancel">
The tool type to delete.
</ParamField>

### Query parameters

<ParamField query="configId" param-type="string" required="false">
Deletes a specific config when provided. Omit it to delete all configs of this type.
</ParamField>

## Billing estimate

Fetch an estimated credits-per-minute cost based on the assistant's provider settings and the active cost catalog.

### Endpoint

`GET /api/assistants/{assistantId}/billing/estimate`

### Path parameters

<ParamField path="assistantId" param-type="string" required="true">
The assistant UUID.
</ParamField>

### Query parameters

<ParamField query="accountId" param-type="string" required="true">
Account UUID.
</ParamField>

<ParamField query="llm_provider" param-type="string" required="false">
Override the LLM provider for live preview.
</ParamField>

<ParamField query="llm_model" param-type="string" required="false">
Override the LLM model for live preview.
</ParamField>

<ParamField query="stt_provider" param-type="string" required="false">
Override the STT provider for live preview.
</ParamField>

<ParamField query="stt_model" param-type="string" required="false">
Override the STT model for live preview.
</ParamField>

<ParamField query="tts_provider" param-type="string" required="false">
Override the TTS provider for live preview.
</ParamField>

<ParamField query="tts_model" param-type="string" required="false">
Override the TTS model for live preview.
</ParamField>

<ParamField query="is_realtime" param-type="string" required="false">
Set to `true` or `1` to estimate realtime mode.
</ParamField>

### Response fields

<ResponseField name="catalog" field-type="object" required="true">
Active cost catalog used for the estimate. Includes `id` and `name`.
</ResponseField>

<ResponseField name="assumptions" field-type="object" required="true">
Assumptions used to convert provider rates into per-minute estimates. Includes `chars_per_minute`, `prompt_tokens_per_minute`, and `completion_tokens_per_minute`.
</ResponseField>

<ResponseField name="assistant" field-type="object" required="true">
Assistant provider and model information used for the estimate, including STT, LLM, and TTS settings.
</ResponseField>

<ResponseField name="rates" field-type="object" required="true">
Resolved provider rates used to build the estimate, including per-minute and per-1k rates.
</ResponseField>

<ResponseField name="breakdown_credits_per_minute" field-type="object" required="true">
Estimated credits-per-minute contribution from STT, TTS, LLM, and telephony components.
</ResponseField>

<ResponseField name="estimated_credits_per_minute" field-type="number" required="true">
Total estimated credits per minute.
</ResponseField>

## Common response shape

All mutation endpoints on this page return a top-level `success` field. Depending on the operation, the response also includes either an `assistant` object or a `message`.

### Success fields

<ResponseField name="success" field-type="boolean" required="true">
Indicates whether the request completed successfully.
</ResponseField>

<ResponseField name="assistant" field-type="object" required="false">
Returned by the create, update, and clone endpoints.
</ResponseField>

<ResponseField name="message" field-type="string" required="false">
Returned by the delete endpoint.
</ResponseField>

## Endpoint summary

| Method | Path | Purpose |
| --- | --- | --- |
| `POST` | ` /api/assistants ` | Create an assistant |
| `PATCH` | ` /api/assistants/{assistantId} ` | Update an assistant |
| `DELETE` | ` /api/assistants/{assistantId} ` | Delete an assistant |
| `POST` | ` /api/assistants/{assistantId}/clone ` | Clone an assistant |
| `GET` | ` /api/assistants/{assistantId}/usage-stats ` | Retrieve usage statistics |
| `GET` | `/api/assistants/{assistantId}/tool-configs` | List tool configurations |
| `GET` | `/api/assistants/{assistantId}/tool-configs/{toolType}` | Get a specific tool configuration |
| `POST` | `/api/assistants/{assistantId}/tool-configs/{toolType}` | Create or update a tool configuration |
| `DELETE` | `/api/assistants/{assistantId}/tool-configs/{toolType}` | Delete tool configuration(s) |
| `GET` | `/api/assistants/{assistantId}/billing/estimate` | Estimate credits per minute |