---
title: Phone Numbers
description: Search, purchase, list, retrieve, and release Talkturo phone numbers, with request formats, auth options, and carrier webhook details.
---

## Manage phone numbers

Use the phone number endpoints to search carrier inventory, provision numbers to a team, list purchased numbers, inspect an existing number, and release numbers you no longer need. This page also documents the carrier webhook endpoints Talkturo uses to keep KYC and compliance status in sync.

<Callout kind="info">

Phone number management endpoints support Talkturo API authentication through an API key, bearer session token, or browser cookie session. The two webhook endpoints are exceptions because carriers call them directly.

</Callout>

## Endpoint summary

| Method | Path | Auth | Purpose |
| --- | --- | --- | --- |
| `POST` | `/api/phone-numbers/search` | API key or session | Search available numbers from the carrier |
| `POST` | `/api/phone-numbers/purchase` | API key or session | Purchase and provision a phone number |
| `GET` | `/api/phone-numbers/purchase` | None | List purchased phone numbers for a team |
| `GET` | `/api/phone-numbers/{id}` | API key or session | Retrieve a specific phone number |
| `DELETE` | `/api/phone-numbers/{id}` | API key or session | Release a phone number |
| `POST` | `/api/phone-numbers/telnyx-webhook` | Carrier signature | Receive Telnyx KYC status updates |
| `POST` | `/api/phone-numbers/twilio-webhook` | None | Receive Twilio bundle status callbacks |

## Search available phone numbers

Search returns numbers currently available from the selected carrier after Talkturo applies country, locality, number type, and compliance checks.

<Request tabs="cURL,JavaScript" default-tab="1">
```bash
curl -X POST "https://app.talkturo.com/api/phone-numbers/search" \
  -H "Authorization: Bearer tk_live_example_9xmk7q2r" \
  -H "Content-Type: application/json" \
  -d '{
    "filters": {
      "countryCode": "US",
      "areaCode": "415",
      "numberType": "local",
      "limit": 5
    },
    "teamId": "team_8f3d2b7c",
    "carrier": "talkturo",
    "provider": "talkturo"
  }'
```

```javascript
const response = await fetch("https://app.talkturo.com/api/phone-numbers/search", {
  method: "POST",
  headers: {
    "Authorization": "Bearer tk_live_example_9xmk7q2r",
    "Content-Type": "application/json"
  },
  body: JSON.stringify({
    filters: {
      countryCode: "US",
      areaCode: "415",
      numberType: "local",
      limit: 5
    },
    teamId: "team_8f3d2b7c",
    carrier: "talkturo",
    provider: "talkturo"
  })
});

const data = await response.json();
console.log(data);
```
</Request>

<Response tabs="200">
```json
{
  "success": true,
  "numbers": [
    {
      "phoneNumber": "+14155550182",
      "country": "US",
      "type": "local",
      "locality": "San Francisco",
      "region": "CA"
    },
    {
      "phoneNumber": "+14155550347",
      "country": "US",
      "type": "local",
      "locality": "San Francisco",
      "region": "CA"
    }
  ],
  "carrier": "talkturo",
  "provider": "talkturo",
  "pricing": {
    "upfrontCredits": 200,
    "monthlyCredits": 100,
    "upfrontUsd": 2,
    "monthlyUsd": 1
  },
  "billing": "credits",
  "verifiedLocalities": [
    "San Francisco"
  ],
  "count": 2
}
```
</Response>

### Request body

<ParamField body="filters" param-type="object" required="true">
Search filters applied before Talkturo queries carrier inventory.
</ParamField>

<ParamField body="filters.countryCode" param-type="string" required="true">
Two-letter country code to search, such as `US`.
</ParamField>

<ParamField body="filters.areaCode" param-type="string" required="false">
Area code filter for local numbers when the carrier supports locality-based search.
</ParamField>

<ParamField body="filters.numberType" param-type="string" required="false" enum="local,toll-free,mobile">
Requested number type.
</ParamField>

<ParamField body="filters.limit" param-type="number" required="false">
Maximum number of results to return.
</ParamField>

<ParamField body="teamId" param-type="string" required="false">
Team identifier used for eligibility and compliance checks during search.
</ParamField>

<ParamField body="carrier" param-type="string" required="false">
Carrier routing key, such as `talkturo`, `talkturo_2`, or a Twilio-backed carrier value.
</ParamField>

<ParamField body="provider" param-type="string" required="false" enum="talkturo,twilio">
Underlying provider used to fulfill the search.
</ParamField>

### Response fields

<ResponseField name="success" field-type="boolean" required="true">
Returns `true` when the search request succeeds.
</ResponseField>

<ResponseField name="numbers" field-type="array" required="true">
Available phone numbers returned after Talkturo applies search filters and carrier checks.
</ResponseField>

<ResponseField name="numbers[].phoneNumber" field-type="string" required="true">
Purchasable phone number in E.164 format.
</ResponseField>

<ResponseField name="numbers[].country" field-type="string" required="false">
Country associated with the returned number.
</ResponseField>

<ResponseField name="numbers[].type" field-type="string" required="false">
Carrier number type, such as `local`, `toll-free`, or `mobile`.
</ResponseField>

<ResponseField name="carrier" field-type="string" required="true">
Carrier that supplied the search results.
</ResponseField>

<ResponseField name="provider" field-type="string" required="true">
Provider used for the search request.
</ResponseField>

<ResponseField name="pricing" field-type="object" required="true">
Pricing summary for numbers in the result set.
</ResponseField>

<ResponseField name="pricing.upfrontCredits" field-type="number" required="false">
One-time upfront charge in credits when billing uses Talkturo credits.
</ResponseField>

<ResponseField name="pricing.monthlyCredits" field-type="number" required="false">
Recurring monthly charge in credits when billing uses Talkturo credits.
</ResponseField>

<ResponseField name="pricing.upfrontUsd" field-type="number" required="false">
One-time upfront charge in USD.
</ResponseField>

<ResponseField name="pricing.monthlyUsd" field-type="number" required="false">
Recurring monthly charge in USD.
</ResponseField>

<ResponseField name="billing" field-type="string" required="true">
Billing mode for the returned inventory. Typical values are `credits`, `exempt`, or `byoa`.
</ResponseField>

<ResponseField name="verifiedLocalities" field-type="array" required="false">
Localities already verified for the requesting team when locality-level compliance applies.
</ResponseField>

<ResponseField name="count" field-type="number" required="true">
Total number of numbers returned in the response.
</ResponseField>

<Callout kind="info">

Talkturo blocks searches in regulated countries until the required verification is complete. Twilio-backed searches also check whether the requested country is verified for the team.

</Callout>

## Purchase a phone number

Purchase provisions a phone number to a team, charges any upfront amount, and returns both the created record and the team's current eligibility details.

<Request tabs="cURL,JavaScript" default-tab="1">
```bash
curl -X POST "https://app.talkturo.com/api/phone-numbers/purchase" \
  -H "Authorization: Bearer tk_live_example_9xmk7q2r" \
  -H "Content-Type: application/json" \
  -d '{
    "phoneNumber": "+14155550182",
    "teamId": "team_8f3d2b7c",
    "carrier": "talkturo",
    "provider": "talkturo",
    "numberType": "local",
    "region": {
      "state": "CA",
      "locality": "San Francisco",
      "rateCenter": "SNFC CNTRL"
    }
  }'
```

```javascript
const response = await fetch("https://app.talkturo.com/api/phone-numbers/purchase", {
  method: "POST",
  headers: {
    "Authorization": "Bearer tk_live_example_9xmk7q2r",
    "Content-Type": "application/json"
  },
  body: JSON.stringify({
    phoneNumber: "+14155550182",
    teamId: "team_8f3d2b7c",
    carrier: "talkturo",
    provider: "talkturo",
    numberType: "local",
    region: {
      state: "CA",
      locality: "San Francisco",
      rateCenter: "SNFC CNTRL"
    }
  })
});

const data = await response.json();
console.log(data);
```
</Request>

<Response tabs="200,402,403,409" default-tab="1">
```json
{
  "success": true,
  "phoneNumber": {
    "id": "pn_7c9c4f2a61d1",
    "phone_number": "+14155550182",
    "status": "active",
    "is_trial": false,
    "trial_expires_at": null,
    "message": "Phone number provisioned successfully"
  },
  "eligibility": {
    "currentCount": 3,
    "maxAllowed": 10,
    "hasSubscription": true,
    "planName": "Growth"
  }
}
```

```json
{
  "success": false,
  "message": "Insufficient credits to purchase this phone number"
}
```

```json
{
  "success": false,
  "message": "Phone number purchase blocked by compliance or plan limits"
}
```

```json
{
  "success": false,
  "message": "Phone number already exists for this team"
}
```
</Response>

### Request body

<ParamField body="phoneNumber" param-type="string" required="true">
Phone number to provision in E.164 format.
</ParamField>

<ParamField body="teamId" param-type="string" required="true">
Team that will own the purchased number.
</ParamField>

<ParamField body="connectionId" param-type="string" required="false">
Connection to associate with the purchased number when your telephony setup uses explicit connection routing.
</ParamField>

<ParamField body="carrier" param-type="string" required="false">
Carrier routing key used to fulfill the purchase.
</ParamField>

<ParamField body="provider" param-type="string" required="false" enum="talkturo,twilio">
Underlying provider used to provision the number.
</ParamField>

<ParamField body="numberType" param-type="string" required="false" enum="local,toll-free,mobile">
Number type expected during provisioning.
</ParamField>

<ParamField body="region" param-type="object" required="false">
Regional metadata used for locality-aware purchases.
</ParamField>

<ParamField body="region.state" param-type="string" required="false">
State or province code for the requested locality.
</ParamField>

<ParamField body="region.locality" param-type="string" required="false">
City or locality tied to the requested number.
</ParamField>

<ParamField body="region.rateCenter" param-type="string" required="false">
Carrier rate center used for more precise local inventory matching.
</ParamField>

### Response fields

<ResponseField name="success" field-type="boolean" required="true">
Returns `true` when provisioning succeeds.
</ResponseField>

<ResponseField name="phoneNumber" field-type="object" required="true">
Provisioned phone number record.
</ResponseField>

<ResponseField name="phoneNumber.id" field-type="string" required="true">
Talkturo identifier for the phone number record.
</ResponseField>

<ResponseField name="phoneNumber.phone_number" field-type="string" required="true">
Provisioned phone number value.
</ResponseField>

<ResponseField name="phoneNumber.status" field-type="string" required="false">
Current lifecycle status for the number.
</ResponseField>

<ResponseField name="phoneNumber.is_trial" field-type="boolean" required="false">
Indicates whether the number is currently in a trial period.
</ResponseField>

<ResponseField name="phoneNumber.trial_expires_at" field-type="string" required="false">
Trial expiration timestamp when the number is provisioned as a trial number.
</ResponseField>

<ResponseField name="phoneNumber.message" field-type="string" required="false">
Provisioning status message returned by the purchase flow.
</ResponseField>

<ResponseField name="eligibility" field-type="object" required="true">
Current phone number eligibility snapshot for the team.
</ResponseField>

<ResponseField name="eligibility.currentCount" field-type="number" required="false">
Current number of phone numbers assigned to the team.
</ResponseField>

<ResponseField name="eligibility.maxAllowed" field-type="number" required="false">
Maximum number of phone numbers the team can hold.
</ResponseField>

<ResponseField name="eligibility.hasSubscription" field-type="boolean" required="false">
Whether the team has an active subscription that permits phone numbers.
</ResponseField>

<ResponseField name="eligibility.planName" field-type="string" required="false">
Subscription plan name used for eligibility checks.
</ResponseField>

### Provisioning notes

Talkturo may poll carrier order state before returning a final success response. For Telnyx-backed orders, the purchase flow can poll for up to ten attempts at three-second intervals.

<Callout kind="alert">

Purchase requests can fail with `402` for insufficient credits, `403` for KYC, plan-limit, or city-verification blocks, and `409` when the team already owns the number.

</Callout>

## List purchased phone numbers

List returns the phone numbers already provisioned for a team.

<Request tabs="cURL,JavaScript" default-tab="1">
```bash
curl -G "https://app.talkturo.com/api/phone-numbers/purchase" \
  --data-urlencode "teamId=team_8f3d2b7c"
```

```javascript
const params = new URLSearchParams({
  teamId: "team_8f3d2b7c"
});

const response = await fetch(
  `https://app.talkturo.com/api/phone-numbers/purchase?${params.toString()}`,
  {
    method: "GET"
  }
);

const data = await response.json();
console.log(data);
```
</Request>

<Response tabs="200">
```json
{
  "success": true,
  "numbers": [
    {
      "id": "pn_7c9c4f2a61d1",
      "phone_number": "+14155550182",
      "status": "active",
      "team_id": "team_8f3d2b7c"
    },
    {
      "id": "pn_4a2de7109c55",
      "phone_number": "+14155550347",
      "status": "active",
      "team_id": "team_8f3d2b7c"
    }
  ],
  "count": 2
}
```
</Response>

### Query parameters

<ParamField query="teamId" param-type="string" required="true">
Team identifier used to list purchased phone numbers.
</ParamField>

### Response fields

<ResponseField name="success" field-type="boolean" required="true">
Returns `true` when the list request succeeds.
</ResponseField>

<ResponseField name="numbers" field-type="array" required="true">
Phone number records assigned to the requested team.
</ResponseField>

<ResponseField name="count" field-type="number" required="true">
Total number of phone numbers returned.
</ResponseField>

## Retrieve a phone number

Retrieve a single phone number record by its Talkturo identifier.

<Request tabs="cURL,JavaScript" default-tab="1">
```bash
curl "https://app.talkturo.com/api/phone-numbers/pn_7c9c4f2a61d1" \
  -H "Authorization: Bearer tk_live_example_9xmk7q2r"
```

```javascript
const response = await fetch(
  "https://app.talkturo.com/api/phone-numbers/pn_7c9c4f2a61d1",
  {
    method: "GET",
    headers: {
      "Authorization": "Bearer tk_live_example_9xmk7q2r"
    }
  }
);

const data = await response.json();
console.log(data);
```
</Request>

<Response tabs="200">
```json
{
  "success": true,
  "phoneNumber": {
    "id": "pn_7c9c4f2a61d1",
    "phone_number": "+14155550182",
    "status": "active",
    "team_id": "team_8f3d2b7c"
  }
}
```
</Response>

### Path parameters

<ParamField path="id" param-type="string" required="true">
Unique phone number identifier.
</ParamField>

### Response fields

<ResponseField name="success" field-type="boolean" required="true">
Returns `true` when the record is found and the request succeeds.
</ResponseField>

<ResponseField name="phoneNumber" field-type="object" required="true">
Phone number record associated with the requested identifier.
</ResponseField>

<ResponseField name="phoneNumber.id" field-type="string" required="true">
Unique identifier for the phone number record.
</ResponseField>

<ResponseField name="phoneNumber.phone_number" field-type="string" required="true">
Stored phone number value.
</ResponseField>

<ResponseField name="phoneNumber.status" field-type="string" required="false">
Current status of the number in Talkturo.
</ResponseField>

## Release a phone number

Release removes a provisioned number so it is no longer assigned to your team. Use this endpoint when you no longer need the number.

<Request tabs="cURL,JavaScript" default-tab="1">
```bash
curl -X DELETE "https://app.talkturo.com/api/phone-numbers/pn_7c9c4f2a61d1" \
  -H "Authorization: Bearer tk_live_example_9xmk7q2r"
```

```javascript
const response = await fetch(
  "https://app.talkturo.com/api/phone-numbers/pn_7c9c4f2a61d1",
  {
    method: "DELETE",
    headers: {
      "Authorization": "Bearer tk_live_example_9xmk7q2r"
    }
  }
);

const data = await response.json();
console.log(data);
```
</Request>

<Response tabs="200">
```json
{
  "success": true,
  "message": "Phone number released"
}
```
</Response>

### Path parameters

<ParamField path="id" param-type="string" required="true">
Unique phone number identifier to release.
</ParamField>

### Response fields

<ResponseField name="success" field-type="boolean" required="true">
Returns `true` when the release request succeeds.
</ResponseField>

<ResponseField name="message" field-type="string" required="true">
Human-readable confirmation that the number was released.
</ResponseField>

## Telnyx webhook

Telnyx calls this endpoint when KYC or requirement group status changes. Talkturo verifies the request signature, extracts the `requirement_group_id`, and updates the matching compliance request.

<Callout kind="alert">

This endpoint requires ED25519 signature verification using the `TELNYX_PUBLIC_KEY` environment variable. If the public key is not configured, the endpoint returns `503`. If signature verification fails, the endpoint returns `400`.

</Callout>

### Endpoint behavior

- **Method:** `POST`
- **Path:** `/api/phone-numbers/telnyx-webhook`
- **Authentication:** Telnyx ED25519 signature verification
- **Body format:** Raw JSON body from Telnyx
- **Retry behavior:** After successful verification and processing, the endpoint always returns `200` to prevent carrier retries

### Request body

<ParamField body="Telnyx event payload" param-type="object" required="true">
Raw Telnyx event JSON. Talkturo reads the raw request body to verify the signature before parsing the payload.
</ParamField>

### Response fields

<ResponseField name="received" field-type="boolean" required="true">
Returns `true` after Talkturo accepts the webhook request for processing.
</ResponseField>

<ResponseField name="status" field-type="string" required="false">
Current requirement group or compliance status after synchronization.
</ResponseField>

<ResponseField name="updated" field-type="boolean" required="false">
Indicates whether Talkturo updated a matching compliance record.
</ResponseField>

<ResponseField name="matchedRequest" field-type="boolean" required="false">
Indicates whether the webhook matched an existing compliance request in Talkturo.
</ResponseField>

## Twilio webhook

Twilio calls this endpoint when bundle status changes. Talkturo does not trust the posted status value directly and instead re-fetches the current bundle status from Twilio before updating internal records.

<Callout kind="info">

This endpoint does not require a signature header. For security, Talkturo reads `BundleSID`, `BundleSid`, or `bundle_sid` from the form-encoded payload and verifies the current state by querying Twilio directly.

</Callout>

### Endpoint behavior

- **Method:** `POST`
- **Path:** `/api/phone-numbers/twilio-webhook`
- **Authentication:** None
- **Body format:** Form-encoded callback payload
- **Retry behavior:** The endpoint always returns `200` to prevent Twilio retries

### Request body

<ParamField body="BundleSID" param-type="string" required="false">
Twilio bundle identifier. The endpoint also accepts `BundleSid` and `bundle_sid`.
</ParamField>

<ParamField body="BundleSid" param-type="string" required="false">
Alternative Twilio field name for the bundle identifier.
</ParamField>

<ParamField body="bundle_sid" param-type="string" required="false">
Lowercase variant of the Twilio bundle identifier field.
</ParamField>

### Response fields

<ResponseField name="received" field-type="boolean" required="true">
Returns `true` after Talkturo accepts the callback.
</ResponseField>

<ResponseField name="status" field-type="string" required="false">
Bundle status retrieved directly from Twilio.
</ResponseField>

<ResponseField name="matchedRequest" field-type="boolean" required="false">
Indicates whether the callback matched an existing compliance request in Talkturo.
</ResponseField>

## Common response patterns

Most phone number endpoints return a success envelope around the resource payload.

### Success envelope

```json
{
  "success": true
}
```

### Webhook acknowledgement envelope

```json
{
  "received": true
}
```

## Related API areas

<Columns cols={2}>
  <Card title="Telephony" href="/en/api-reference/telephony" icon="phone-call" cta="View endpoints" />
  <Card title="Assistants" href="/en/api-reference/assistants" icon="users" cta="View endpoints" />
</Columns>