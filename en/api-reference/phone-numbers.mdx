---
title: Phone Numbers
description: Search, purchase, retrieve, and release phone numbers for Talkturo telephony, plus carrier webhook endpoints for KYC and compliance status updates.
---

## Manage phone numbers

Use the phone number endpoints to search carrier inventory, provision numbers to an account, inspect an existing number, and release numbers you no longer need. This page also documents the carrier webhook endpoints Talkturo uses to keep KYC and compliance status in sync.

<Callout kind="info">

Phone number search and purchase support an authenticated session cookie or API key authentication. The two webhook endpoints are exceptions because carriers call them directly.

</Callout>

## Endpoint summary

| Method | Path | Auth | Purpose |
| --- | --- | --- | --- |
| `POST` | `/api/phone-numbers/search` | Session or API key | Search available numbers from the carrier |
| `POST` | `/api/phone-numbers/purchase` | Session or API key | Purchase and provision a phone number |
| `GET` | `/api/phone-numbers/purchase` | None | List purchased phone numbers for a team |
| `GET` | `/api/phone-numbers/{id}` | Session cookie | Retrieve a specific phone number |
| `DELETE` | `/api/phone-numbers/{id}` | Session cookie | Release a phone number |
| `POST` | `/api/phone-numbers/telnyx-webhook` | Carrier signature | Receive Telnyx KYC status updates |
| `POST` | `/api/phone-numbers/twilio-webhook` | None | Receive Twilio bundle status callbacks |

## Search available phone numbers

Search returns numbers currently available from the configured carrier. Send filters in the JSON body instead of the query string.

<Request tabs="cURL,JavaScript" default-tab="1">
```bash
curl -X POST "https://app.talkturo.com/api/phone-numbers/search" \
  -H "Content-Type: application/json" \
  -H "Cookie: sb-session=sb-session-example-a92k3m" \
  -d '{
    "filters": {
      "countryCode": "US",
      "numberType": "local",
      "areaCode": "415"
    },
    "teamId": "6f2c3d6d-1d6a-4af9-9a8b-2f8f3f1f4d22",
    "carrier": "talkturo_1"
  }'
```

```javascript
const response = await fetch("https://app.talkturo.com/api/phone-numbers/search", {
  method: "POST",
  credentials: "include",
  headers: {
    "Content-Type": "application/json"
  },
  body: JSON.stringify({
    filters: {
      countryCode: "US",
      numberType: "local",
      areaCode: "415"
    },
    teamId: "6f2c3d6d-1d6a-4af9-9a8b-2f8f3f1f4d22",
    carrier: "talkturo_1"
  })
});

const data = await response.json();
console.log(data);
```
</Request>

<Response tabs="200,403,409,502">
```json
{
  "success": true,
  "numbers": [
    {
      "phoneNumber": "+14155550182",
      "country": "US",
      "type": "local",
      "features": ["sms", "voice"],
      "verifiedLocalities": [
        {
          "state": "CA",
          "locality": "San Francisco",
          "rateCenter": "SNFC CNTRL"
        }
      ],
      "pricing": {
        "upfrontUsd": "1.00",
        "monthlyUsd": "1.00",
        "upfrontCredits": 100,
        "monthlyCredits": 100,
        "estimated": false
      },
      "billing": "credits"
    }
  ],
  "carrier": "talkturo_1",
  "provider": "talkturo",
  "verifiedLocalities": [
    {
      "state": "CA",
      "locality": "San Francisco",
      "rateCenter": "SNFC CNTRL"
    }
  ],
  "count": 1
}
```

```json
{
  "success": false,
  "requiresVerification": true,
  "countryCode": "US",
  "numberType": "local",
  "message": "KYC verification is required before searching this inventory."
}
```

```json
{
  "success": false,
  "message": "Requested carrier is unavailable."
}
```

```json
{
  "success": false,
  "message": "Carrier search failed."
}
```
</Response>

### Request body

<ParamField body="filters" param-type="object" required="true">
Search filters for the carrier inventory request.
</ParamField>

<ParamField body="teamId" param-type="string" required="false">
Workspace identifier used for KYC gating and billing. Pass a UUID when you need results scoped to a specific team.
</ParamField>

<ParamField body="carrier" param-type="string" required="false">
Carrier alias to query, such as `talkturo_1`, `talkturo_2`, or `own_twilio`.
</ParamField>

<ParamField body="provider" param-type="string" required="false">
Legacy alias for `carrier`. Use this only if you still rely on older client integrations.
</ParamField>

### Filter fields

<ParamField body="filters.countryCode" param-type="string" required="true">
Two-letter ISO 3166-1 alpha-2 country code, such as `US`.
</ParamField>

<ParamField body="filters.numberType" param-type="string" required="false" enum="local,mobile,toll-free">
Number inventory type to search.
</ParamField>

<ParamField body="filters.areaCode" param-type="string" required="false">
Three-digit area code for locality-based searches.
</ParamField>

### Response fields

<ResponseField name="success" field-type="boolean" required="true">
Returns `true` when the search request succeeds.
</ResponseField>

<ResponseField name="numbers" field-type="array" required="true">
List of available numbers returned by the carrier after filters are applied.
</ResponseField>

<ResponseField name="numbers[].phoneNumber" field-type="string" required="true">
The purchasable phone number in E.164 format.
</ResponseField>

<ResponseField name="numbers[].country" field-type="string" required="false">
Country associated with the returned number.
</ResponseField>

<ResponseField name="numbers[].type" field-type="string" required="false">
Carrier number type, such as `local`, `mobile`, or `toll-free`.
</ResponseField>

<ResponseField name="numbers[].features" field-type="array" required="false">
Capabilities available on the number, such as `sms` or `voice`.
</ResponseField>

<ResponseField name="numbers[].verifiedLocalities" field-type="array" required="false">
Locality metadata the carrier verified for the number.
</ResponseField>

<ResponseField name="numbers[].pricing" field-type="object" required="false">
Pricing details for the returned number.
</ResponseField>

<ResponseField name="numbers[].billing" field-type="string" required="false">
Billing model for the number. Returned values include `credits`, `exempt`, or `byoa`.
</ResponseField>

<ResponseField name="carrier" field-type="string" required="false">
Carrier alias that fulfilled the search request.
</ResponseField>

<ResponseField name="provider" field-type="string" required="false">
Normalized provider name. Returned values are `talkturo` or `twilio`.
</ResponseField>

<ResponseField name="verifiedLocalities" field-type="array" required="false">
Verified locality data returned at the response level for the selected inventory set.
</ResponseField>

<ResponseField name="count" field-type="number" required="true">
Number of results returned in `numbers`.
</ResponseField>

### Error responses

<Callout kind="alert">

Search can fail before inventory is returned:
- `400` when `filters.countryCode` is missing
- `403` when KYC verification is required for the requested country or number type
- `409` when the requested carrier is unavailable
- `502` when the carrier returns an upstream error

</Callout>

## Purchase a phone number

Purchase provisions a number to a team. The request body always includes the selected phone number, and session-based requests also require `teamId`.

<Request tabs="cURL,JavaScript" default-tab="1">
```bash
curl -X POST "https://app.talkturo.com/api/phone-numbers/purchase" \
  -H "Content-Type: application/json" \
  -H "Cookie: sb-session=sb-session-example-a92k3m" \
  -d '{
    "phoneNumber": "+14155550182",
    "teamId": "6f2c3d6d-1d6a-4af9-9a8b-2f8f3f1f4d22",
    "carrier": "talkturo_1",
    "numberType": "local",
    "region": {
      "state": "CA",
      "locality": "San Francisco",
      "rateCenter": "SNFC CNTRL"
    }
  }'
```

```javascript
const response = await fetch("https://app.talkturo.com/api/phone-numbers/purchase", {
  method: "POST",
  credentials: "include",
  headers: {
    "Content-Type": "application/json"
  },
  body: JSON.stringify({
    phoneNumber: "+14155550182",
    teamId: "6f2c3d6d-1d6a-4af9-9a8b-2f8f3f1f4d22",
    carrier: "talkturo_1",
    numberType: "local",
    region: {
      state: "CA",
      locality: "San Francisco",
      rateCenter: "SNFC CNTRL"
    }
  })
});

const data = await response.json();
console.log(data);
```
</Request>

<Response tabs="200,402,403,409,502">
```json
{
  "success": true,
  "phoneNumber": {
    "id": "pn_7c9c4f2a61d1",
    "phone_number": "+14155550182",
    "status": "active",
    "is_trial": false,
    "trial_expires_at": null,
    "message": "Phone number provisioned successfully."
  },
  "eligibility": {
    "currentCount": 3,
    "maxAllowed": 10,
    "hasSubscription": true,
    "planName": "Growth"
  }
}
```

```json
{
  "success": false,
  "balance": 250,
  "required": 500,
  "message": "Insufficient credits to purchase this number."
}
```

```json
{
  "success": false,
  "requiresVerification": true,
  "message": "KYC verification is required before purchasing this number."
}
```

```json
{
  "success": false,
  "message": "Phone number is already owned by this account."
}
```

```json
{
  "success": false,
  "message": "Provider provisioning failed."
}
```
</Response>

### Request body

<ParamField body="phoneNumber" param-type="string" required="true">
The phone number to provision in E.164 format, such as `+14155551234`.
</ParamField>

<ParamField body="teamId" param-type="string" required="false">
Workspace UUID that will own the purchased number. This field is required for session-authenticated requests and falls back to the authenticated account for API key requests.
</ParamField>

<ParamField body="connectionId" param-type="string" required="false">
Telnyx FQDN connection identifier to associate during provisioning.
</ParamField>

<ParamField body="carrier" param-type="string" required="false">
Carrier alias to use for the purchase request.
</ParamField>

<ParamField body="provider" param-type="string" required="false">
Legacy alias for `carrier`.
</ParamField>

<ParamField body="numberType" param-type="string" required="false" enum="local,mobile,toll-free">
Number type used for KYC scoping and carrier handling.
</ParamField>

<ParamField body="region" param-type="object" required="false">
Display-only locality metadata for the selected number.
</ParamField>

### Response fields

<ResponseField name="success" field-type="boolean" required="true">
Returns `true` when provisioning succeeds.
</ResponseField>

<ResponseField name="phoneNumber" field-type="object" required="true">
The newly provisioned phone number record.
</ResponseField>

<ResponseField name="phoneNumber.id" field-type="string" required="true">
Talkturo identifier for the phone number record.
</ResponseField>

<ResponseField name="phoneNumber.phone_number" field-type="string" required="true">
Provisioned phone number value.
</ResponseField>

<ResponseField name="phoneNumber.status" field-type="string" required="false">
Current lifecycle status for the number, such as `active`.
</ResponseField>

<ResponseField name="phoneNumber.is_trial" field-type="boolean" required="false">
Indicates whether the number is currently in a trial period.
</ResponseField>

<ResponseField name="phoneNumber.trial_expires_at" field-type="string" required="false">
Timestamp when the trial expires, if applicable.
</ResponseField>

<ResponseField name="phoneNumber.message" field-type="string" required="false">
Provider or provisioning message associated with the number.
</ResponseField>

<ResponseField name="eligibility" field-type="object" required="false">
Plan and entitlement information evaluated during purchase.
</ResponseField>

<ResponseField name="eligibility.currentCount" field-type="number" required="false">
Current number of provisioned phone numbers for the team or account.
</ResponseField>

<ResponseField name="eligibility.maxAllowed" field-type="number" required="false">
Maximum number of phone numbers allowed by the current plan.
</ResponseField>

<ResponseField name="eligibility.hasSubscription" field-type="boolean" required="false">
Indicates whether the team has an active subscription.
</ResponseField>

<ResponseField name="eligibility.planName" field-type="string" required="false">
Name of the current subscription plan.
</ResponseField>

### Provisioning notes

Carrier requirements vary by country, number type, and regulatory flow. If the carrier requires compliance data, include those fields in the same purchase request body.

<Callout kind="alert">

Purchase can return these non-success responses:
- `400` when required parameters are missing
- `402` when the account does not have enough credits, including `balance` and `required`
- `403` when the plan limit is exceeded or KYC verification is required
- `409` when the number is already owned
- `500` or `502` when the provider returns an upstream error

</Callout>

## List purchased phone numbers

Use the purchase listing endpoint to return phone numbers already provisioned for a team.

<Request tabs="cURL,JavaScript" default-tab="1">
```bash
curl -G "https://app.talkturo.com/api/phone-numbers/purchase" \
  --data-urlencode "teamId=6f2c3d6d-1d6a-4af9-9a8b-2f8f3f1f4d22"
```

```javascript
const params = new URLSearchParams({
  teamId: "6f2c3d6d-1d6a-4af9-9a8b-2f8f3f1f4d22"
});

const response = await fetch(
  `https://app.talkturo.com/api/phone-numbers/purchase?${params.toString()}`,
  {
    method: "GET"
  }
);

const data = await response.json();
console.log(data);
```
</Request>

<Response tabs="200">
```json
{
  "success": true,
  "numbers": [
    {
      "id": "pn_7c9c4f2a61d1",
      "phone_number": "+14155550182",
      "provider": "talkturo",
      "ownership_type": "managed",
      "provider_account_id": "acct_3n8f2m1q",
      "status": "active",
      "features": ["sms", "voice"],
      "country_code": "US",
      "assistant_id": "asst_4f9c2a1b",
      "created_by_user_id": "usr_91ab32cd",
      "created_at": "2024-09-18T15:22:11.000Z",
      "assistant_name": "Sales line",
      "created_by_email": "jane@acme.com",
      "created_by_name": "Jane Chen"
    }
  ],
  "count": 1
}
```
</Response>

### Query parameters

<ParamField query="teamId" param-type="string" required="true">
Workspace UUID whose purchased phone numbers should be returned.
</ParamField>

### Response fields

<ResponseField name="success" field-type="boolean" required="true">
Returns `true` when the list request succeeds.
</ResponseField>

<ResponseField name="numbers" field-type="array" required="true">
Purchased phone numbers associated with the requested team.
</ResponseField>

<ResponseField name="numbers[].id" field-type="string" required="true">
Talkturo identifier for the phone number record.
</ResponseField>

<ResponseField name="numbers[].phone_number" field-type="string" required="true">
Provisioned phone number value in E.164 format.
</ResponseField>

<ResponseField name="numbers[].provider" field-type="string" required="false">
Provider associated with the number.
</ResponseField>

<ResponseField name="numbers[].ownership_type" field-type="string" required="false">
Ownership model for the number.
</ResponseField>

<ResponseField name="numbers[].provider_account_id" field-type="string" required="false">
Provider-side account identifier associated with the number.
</ResponseField>

<ResponseField name="numbers[].status" field-type="string" required="false">
Current lifecycle status for the number.
</ResponseField>

<ResponseField name="numbers[].features" field-type="array" required="false">
Capabilities enabled for the number.
</ResponseField>

<ResponseField name="numbers[].country_code" field-type="string" required="false">
Two-letter country code for the number.
</ResponseField>

<ResponseField name="numbers[].assistant_id" field-type="string" required="false">
Identifier of the assistant currently assigned to the number, if any.
</ResponseField>

<ResponseField name="numbers[].created_by_user_id" field-type="string" required="false">
User identifier for the creator of the number record.
</ResponseField>

<ResponseField name="numbers[].created_at" field-type="string" required="false">
Timestamp when the number record was created.
</ResponseField>

<ResponseField name="numbers[].assistant_name" field-type="string" required="false">
Assistant name associated with the number, if assigned.
</ResponseField>

<ResponseField name="numbers[].created_by_email" field-type="string" required="false">
Email address of the user who created the number record.
</ResponseField>

<ResponseField name="numbers[].created_by_name" field-type="string" required="false">
Display name of the user who created the number record.
</ResponseField>

<ResponseField name="count" field-type="number" required="true">
Number of records returned in `numbers`.
</ResponseField>

## Retrieve a phone number

Retrieve a single phone number record by its Talkturo identifier.

### Path parameters

<ParamField path="id" param-type="string" required="true">
Unique phone number identifier.
</ParamField>

### Example response

```json
{
  "success": true,
  "phoneNumber": {
    "id": "pn_7c9c4f2a61d1",
    "phoneNumber": "+14155550182",
    "status": "active"
  }
}
```

### Response fields

<ResponseField name="success" field-type="boolean" required="true">
Returns `true` when the record is found and the request succeeds.
</ResponseField>

<ResponseField name="phoneNumber" field-type="object" required="true">
Phone number record associated with the requested identifier.
</ResponseField>

<ResponseField name="phoneNumber.id" field-type="string" required="true">
Unique identifier for the phone number record.
</ResponseField>

<ResponseField name="phoneNumber.phoneNumber" field-type="string" required="true">
Stored phone number value.
</ResponseField>

<ResponseField name="phoneNumber.status" field-type="string" required="false">
Current status of the number in Talkturo.
</ResponseField>

## Release a phone number

Release removes a provisioned number so it is no longer assigned to your account. Use this endpoint when you no longer need the number.

### Path parameters

<ParamField path="id" param-type="string" required="true">
Unique phone number identifier to release.
</ParamField>

### Example response

```json
{
  "success": true,
  "message": "Phone number released"
}
```

### Response fields

<ResponseField name="success" field-type="boolean" required="true">
Returns `true` when the release request succeeds.
</ResponseField>

<ResponseField name="message" field-type="string" required="true">
Human-readable confirmation that the number was released.
</ResponseField>

## Telnyx webhook

Telnyx calls this endpoint when KYC or requirement group status changes. Talkturo verifies the request signature, extracts the `requirement_group_id`, and updates the matching compliance request.

<Callout kind="alert">

This endpoint requires ED25519 signature verification using the `TELNYX_PUBLIC_KEY` environment variable. If the public key is not configured, the endpoint returns `503`. If signature verification fails, the endpoint returns `400`.

</Callout>

### Endpoint behavior

- **Method:** `POST`
- **Path:** `/api/phone-numbers/telnyx-webhook`
- **Authentication:** Telnyx ED25519 signature verification
- **Body format:** Raw JSON body from Telnyx
- **Retry behavior:** After successful verification and processing, the endpoint always returns `200` to prevent carrier retries

### Request body

<ParamField body="Telnyx event payload" param-type="object" required="true">
Raw Telnyx event JSON. Talkturo reads the raw request body to verify the signature before parsing the payload.
</ParamField>

### Response fields

<ResponseField name="received" field-type="boolean" required="true">
Returns `true` after Talkturo accepts the webhook request for processing.
</ResponseField>

<ResponseField name="status" field-type="string" required="false">
Current requirement group or compliance status after synchronization.
</ResponseField>

<ResponseField name="updated" field-type="boolean" required="false">
Indicates whether Talkturo updated a matching compliance record.
</ResponseField>

<ResponseField name="matchedRequest" field-type="boolean" required="false">
Indicates whether the webhook matched an existing compliance request in Talkturo.
</ResponseField>

## Twilio webhook

Twilio calls this endpoint when bundle status changes. Talkturo does not trust the posted status value directly and instead re-fetches the current bundle status from Twilio before updating internal records.

<Callout kind="info">

This endpoint does not require a signature header. For security, Talkturo reads `BundleSID`, `BundleSid`, or `bundle_sid` from the form-encoded payload and verifies the current state by querying Twilio directly.

</Callout>

### Endpoint behavior

- **Method:** `POST`
- **Path:** `/api/phone-numbers/twilio-webhook`
- **Authentication:** None
- **Body format:** Form-encoded callback payload
- **Retry behavior:** The endpoint always returns `200` to prevent Twilio retries

### Request body

<ParamField body="BundleSID" param-type="string" required="false">
Twilio bundle identifier. The endpoint also accepts `BundleSid` and `bundle_sid`.
</ParamField>

<ParamField body="BundleSid" param-type="string" required="false">
Alternative Twilio field name for the bundle identifier.
</ParamField>

<ParamField body="bundle_sid" param-type="string" required="false">
Lowercase variant of the Twilio bundle identifier field.
</ParamField>

### Response fields

<ResponseField name="received" field-type="boolean" required="true">
Returns `true` after Talkturo accepts the callback.
</ResponseField>

<ResponseField name="status" field-type="string" required="false">
Bundle status retrieved directly from Twilio.
</ResponseField>

<ResponseField name="matchedRequest" field-type="boolean" required="false">
Indicates whether the callback matched an existing compliance request in Talkturo.
</ResponseField>

## Common response patterns

Most phone number endpoints return a success envelope around the resource payload.

### Success envelope

```json
{
  "success": true
}
```

### Webhook acknowledgement envelope

```json
{
  "received": true
}
```

## Related API areas

<Columns cols={2}>
  <Card title="Assistant phone numbers" href="/en/api-reference/assistants" icon="phone" cta="View endpoint" />
  <Card title="Assistants" href="/en/api-reference/assistants" icon="users" cta="View endpoints" />
</Columns>